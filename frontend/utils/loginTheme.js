import { createTheme } from '@mui/material/styles'

const loginTheme = createTheme({
  palette: {
    background: {
      default: '#e3ecfe', // e3ecfe  EAEAEA
    }
  }
})

export default loginTheme
